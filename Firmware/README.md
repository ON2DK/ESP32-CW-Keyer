# Firmware

Arduino IDE firmware for the ESP32 CW Keyer will be kept in this folder.

The firmware must match the final PCB pin mapping and support the trainer functions together with the Keyer-specific functions such as CAT, KEY, PTT and the four memory/program buttons.

Before flashing, verify that the GPIO definitions in the sketch correspond to the final KiCad nets.
